# 更新日志

本项目遵循[语义化版本](https://semver.org/lang/zh-CN/)，每个版本的变更都记在这里。

## [1.0.0] - 2026-09-16

首个正式版本：CLI + 网页面板 + 到期提醒，功能完整可用。

### 新增

**订阅管理（`subs.py`，纯标准库 CLI）**
- 增删改查、按到期日排序；已停用项保留数据但不参与提醒
- 周期全支持：`weekly` / `biweekly` / `monthly` / `quarterly` / `semiannual` / `yearly` / `custom_days`
- 多币种，每个币种分开合计；金额可留空（只想被提醒、不想记账）
- 月末锚点不漂移：每月 31 号扣费的，2 月落到 28/29，3 月回到 31（`anchor_day`）
- `renew` 自动顺延一个周期，漏了几期会一直追到未来，`--date` 可指定下次扣费日
- `roll` 把所有「已过期且自动扣费」的一次性顺延；每次续费写入 `history`
- 日期写法宽松：`2026-09-18` / `2026/9/18` / `9/18`（没写年份且已过去 → 自动算明年）
- 数据文件可换：`--file 路径`，环境变量 `SUBS_FILE` 亦可

**到期提醒**
- `subs.py check` 在没东西到期时**零输出**，方便接任意通知渠道而不骚扰
- 过期项不受提醒窗口影响，永远置顶（`⚠️ 已过期 N 天`）
- `examples/reminder-cron.sh`：macOS 通知中心横幅 + Telegram Bot 推送，可二选一

**网页面板（`panel.py`，无前端构建步骤）**
- 看板：订阅数 / 7 天内到期 / 已过期 / 每月·每年折算支出（周付、季付、年付都会折算）
- 每条订阅带周期进度条、剩余天数标签、自动/手动续费标记
- 操作：添加、编辑、已续费（自动顺延）、转手动/转自动、停用/启用、删除、一键顺延过期项
- 快捷键 `n` / `r` / `Esc`；深链 `/#add` 直接弹出新增表单
- 端口被占用自动往后找，不会启动失败

**手机访问**
- `panel.py --host 0.0.0.0`（macOS 可双击 `panel-phone.command`）：打印手机地址并生成二维码 `phone-access.png`
- 自动启用令牌校验，未带令牌的读/写请求一律 401；令牌落盘（`.panel_token`，权限 600）重启不变，二维码长期有效
- 手机打开一次记住令牌（cookie 30 天）；Safari「添加到主屏幕」可当 App 使用（内置图标与全屏 meta）
- 外出可用 Cloudflare Tunnel 暴露本地端口

### 质量

- 63 个单元测试，约 4 秒跑完：`python3 -m unittest discover -s tests -t .`
- 覆盖日期宽松解析与月末钳制、各周期推进、多币种汇总、续费/顺延/历史、提醒窗口边界
  （第 7 天进、第 8 天不进、过期必报）、面板动作层校验、图标 PNG 逐 chunk CRC 与像素解压校验、
  局域网 IP 过滤（排除 VPN 假地址段），并**真起一个 HTTP 服务**验证令牌 401 / 下发 cookie / 写操作权限 / 公开图标
- CI 矩阵：ubuntu py3.8 / 3.9 / 3.11 / 3.13 + macOS 3.12 + Windows 3.12

### 说明

- 纯 Python，无第三方依赖，无二进制产物
- `.command` 双击启动为 macOS 专属；手机访问用到的 `route` / `ipconfig` / `scutil` 也仅在 macOS 生效，
  其他系统走通用兜底（取不到就只打印本机地址）
- `.panel_token`、`subscriptions.json`、`phone-access.png` 已在 `.gitignore` 中，请勿提交
